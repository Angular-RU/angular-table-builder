sudo: required
dist: trusty

language: node_js
node_js:
    - '12'

cache:
    directories:
        - node_modules

install:
    - yarn install --frozen-lockfile --non-interactive

script:
    - yarn lint --cache
    - yarn build:lib
    - yarn build:app
    - yarn test --coverage
