import { FilterableMessage } from './filterable.interface';
import { TableRow } from '../../interfaces/table-builder.external';
export declare function filterAllWorker({ source, global, types, columns }: FilterableMessage): TableRow[];
